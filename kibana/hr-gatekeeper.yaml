apiVersion: helm.fluxcd.io/v1
kind: HelmRelease
metadata:
  name: kibana-gatekeeper
  namespace: kibana
spec:
  releaseName: kibana-gatekeeper
  helmVersion: v3
  chart:
    repository: https://gabibbo97.github.io/charts/
    name: keycloak-gatekeeper
    version: 3.3.1
  values:
    ClientID: kibana
    discoveryURL: https://sso.9ka.nl/auth/realms/9ka
    ingress:
      annotations:
        cert-manager.io/cluster-issuer: letsencrypt
      enabled: true
      hosts:
      - kibana.9ka.nl
      tls:
      - hosts:
        - kibana.9ka.nl
        secretName: kibana-tls
    upstreamURL: http://kibana-kibana:5601/
  valuesFrom:
  - secretKeyRef:
      name: secrets
